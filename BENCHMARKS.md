# 基准记录

本文件只收录能够由保留的配置、代码提交、运行环境和原始日志复核的实验。公开
仓库不记录缺少证据的排行榜成绩，也不将计划中的功能描述为已完成。

## 当前状态

目前尚未发布满足下述复现要求的公开基准结果。

| 实验 | 模态 | 配置 | 代码提交 | 随机种子 | mAP@50 | mAP@75 | mAP@50-95 |
|---|---|---|---|---:|---:|---:|---:|
| 待补充 | RGB + IR + Depth | `configs/default.yaml` | 待补充 | 42 | — | — | — |

`—` 表示没有可公开验证的结果，不表示零分。

## 记录要求

新增一条结果时必须同时保留：

1. 完整配置文件；
2. Git 提交哈希；
3. Python、PyTorch 与关键依赖版本；
4. 数据划分方式及样本数量；
5. 随机种子；
6. TensorBoard 日志或等价的机器可读日志；
7. 最佳权重对应的验证轮次；
8. 推理阈值和是否启用 TTA；
9. 原始评估输出。

## 建议的公开消融

以下实验用于判断模态是否真正贡献有效信息；只有实际运行后才能填写结果：

| 实验 | 目的 |
|---|---|
| RGB | 建立单模态参考 |
| RGB + Infrared | 检查热信息贡献 |
| RGB + Depth | 检查几何信息贡献 |
| RGB + Infrared + Depth | 评估五通道早期融合 |

不同实验应保持数据划分、训练协议、随机种子和后处理一致。只改变输入模态仍可能
改变参数初始化和首层结构，因此单次运行不足以证明稳定提升；条件允许时应报告
多个随机种子的均值与标准差。

## 基准模板

```text
Experiment:
Date:
Commit:
Config:
Dataset split:
Seed:
Environment:
Best epoch:
Inference settings:
mAP@50:
mAP@75:
mAP@50-95:
Log artifact:
Notes:
```
